# MeshLED Docs Site

This directory contains the Astro + Starlight implementation for GitHub Pages.

## Local preview

```bash
cd docs-site
npm install
npm run dev
```

## Build

```bash
cd docs-site
npm run build
```

The GitHub Pages workflow builds this directory and publishes `docs-site/dist`.
